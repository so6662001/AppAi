server:
  port: ${PORT:8090}
  servlet:
    context-path: /api/registry/v1
spring:
  application:
    name: metric-registry-service
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/steel_governance?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8mb4}
    username: ${DB_USER:root}
    password: ${DB_PASS:steeldev}
  jackson:
    default-property-inclusion: NON_NULL
